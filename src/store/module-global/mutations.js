export function setTitle(state, opened) {
  state.title = opened;
}
export function setLoginew(state, opened) {
  state.loginew = opened;
}
export function setdirectionNow(state, opened) {
  state.directionNow = opened;
}
export function setdirectionNowLat(state, opened) {
  state.directionNowLat = opened;
}
export function setdirectionNowLng(state, opened) {
  state.directionNowLng = opened;
}
export function setIdService(state, opened) {
  state.IdService = opened;
}
export function setNameService(state, opened) {
  state.NameService = opened;
}
export function setnegocioSelect(state, opened) {
  state.negocioSelect = opened;
}
export function setshowHeader(state, opened) {
  state.showHeader = opened;
}
