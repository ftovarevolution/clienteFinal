export function setRegister(state, opened) {
  state.register = opened;
}
export function setPassword(state, opened) {
  state.recoverPass = opened;
}
export function setVerify(state, opened) {
  state.verifyCode = opened;
}
export function setUserVerify(state, opened) {
  state.userVerify = opened;
}
